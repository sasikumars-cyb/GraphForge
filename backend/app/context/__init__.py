"""Context package — Entry Resolvers and Context Assembler.

Entry Resolvers map arbitrary entry points (free text, PR references,
Jira stories, ...) to a canonical Subject understood by the Orchestrator.
"""
