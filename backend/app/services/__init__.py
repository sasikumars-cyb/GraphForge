"""Business logic layer.

One module per use case (e.g. `analyze_change.py`) will live here once
implemented. Services depend on `models`/`schemas` and on the interfaces
declared in `graph`, `ai`, and `integrations` - never on a concrete adapter
directly - so those subsystems can be swapped without touching a service.

Empty for now: no business logic has been implemented yet.
"""
