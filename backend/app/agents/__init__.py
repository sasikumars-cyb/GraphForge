"""Agent package — contains the frozen Agent Contract and every agent
implementation. The Agent Contract (_contract.py) is the single artefact
every agent, the Orchestrator, and the API router build against.

No registration logic lives here — see app.orchestrator.registry.
"""
