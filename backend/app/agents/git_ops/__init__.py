"""Git Operations Agents — create_branch and commit_changes.

Deterministic execution agents that apply GeneratedCodeResult artifacts
to GitHub via the Git Data API. No LLM calls — these are pure API
orchestration with idempotency guarantees.
"""
