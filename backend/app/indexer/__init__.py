"""The indexing pipeline: reads a codebase and produces the nodes and edges
that populate the dependency graph in `app.graph`.

Not implemented yet. Once built, this is what `app.integrations`
(fetching source) and `app.graph` (storing the result) sit either side of.
"""
