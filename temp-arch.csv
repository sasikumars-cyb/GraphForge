Architecture Proposal · Not Yet Approved

GraphForge — Dual-Mode Workflow Architecture
A shared workflow engine parameterized by workflow_type, adding a Planning Workflow (human-gated, no repo writes) and an optional Auto Execution Workflow (writes code, opens a PR, and — for the first time — feeds the existing Review Agent a real diff instead of freetext). No implementation in this document.

00
Recommendation, up front
Ship in two decisions, not one
Approve Phases 1–4 now (shared engine + Planning Workflow + Engineering Review + Approval gate). This alone fixes the real bug from the last two audits — Review no longer gets invoked with a freetext subject it can't handle, because Planning Workflow never calls Review at all. It's fully additive, backward-compatible, and carries none of the new risk.

Treat Phases 5–10 (Auto Execution) as a separate approval — that's where all the genuinely new, higher-stakes infrastructure lives: an LLM writing real code, real git writes, and test execution. Details below; recommend reviewing that half on its own once Planning Workflow has shipped and been used.

01
Architecture
Component overview
GitHubVersionControlProvider — extended, same class as existing

New agents

Existing agents — reused unchanged

orchestrator — unchanged

workflow_service.py — shared engine

WORKFLOW_TYPE_STAGES registry (new, data not code)

advance_workflow() — type-aware, one implementation

build_stage_context() — reused unchanged

workflows.py router — extended, one router

POST /workflows

POST /workflows/:id/continue

POST /workflows/:id/approve  (new)

POST /workflows/:id/reject  (new)

Frontend

NewWorkflowPage — type + entry-point selector

WorkflowPage — already stage-driven, needs no change

Dashboard — split counts by type

AgentSelector

AgentRegistry

PlanningAgent

DevelopmentAgent

TestPlanningAgent

ReviewAgentAdapter → InvestigationAgent

EngineeringReviewAgent

CodeGenerationAgent

CreateBranchAgent

CommitChangesAgent

RunTestsAgent

CreatePullRequestAgent

get_diff / list_changed_files (existing, read)

create_branch / commit_files / create_pull_request (new, write)

The engine is genuinely one implementation: advance_workflow(), _build_stages(), and continue_workflow() stop reading the hardcoded STAGES tuple and instead look up WORKFLOW_TYPE_STAGES[workflow.workflow_type] — a data table, not a second code path. Everything downstream of "here is today's ordered stage list" (pipeline rendering, evidence, confidence, artifact cards) is already generic over the stage list, confirmed by re-reading PipelineGraph.tsx/WorkflowHeader.tsx/deriveWorkflowState() from the last two sessions of work — none of them assume 4 fixed stages, they iterate workflow.stages[]. That's the payoff of the single-source-of-truth refactor done earlier: this feature mostly falls out of it for free on the frontend.

02
Workflow lifecycle / state machine
Workflow.status — existing field, vocabulary extended
create_workflow()

continue() advances to next stage

Planning type reaches engineering_review completion

Auto-Execution type reaches ai_pr_review completion

POST /approve

POST /reject

may spawn an Auto-Execution workflow referencing this one

real PR left open on GitHub for human merge

in_progress

awaiting_approval

completed

approved

rejected

State	Meaning	Who can act	Reuses
in_progress	Existing value, unchanged meaning	Human clicks Approve & Continue per stage — same gate as today	ApprovalGateBanner, continue_workflow(), unchanged
awaiting_approval	new	Planning Workflow only, set once engineering_review completes	—
approved / rejected	new	Human, via new endpoints	Same request/response shape as existing continue
completed	Existing value; for Auto-Execution means "PR opened, awaiting merge on GitHub" — we do not build merge-approval, GitHub already has it	—	—
03
Sequence — Planning Workflow
RunCoordinator
workflow_service
workflows router
WorkflowPage
User
RunCoordinator
workflow_service
workflows router
WorkflowPage
User
loop
[development, testing,
engineering_review]
Create Planning Workflow
POST /workflows {workflow_type: planning}
create_workflow(type=planning)
execute(planning)
PlanningPlan persisted as Run
Approve & Continue
POST /workflows/:id/continue
advance + execute next stage (type-aware lookup)
execute(stage)
stage result persisted as Run
engineering_review done -> status = awaiting_approval
Approve Blueprint
POST /workflows/:id/approve
status = approved
04
Sequence — Auto Execution (from an approved blueprint)
ReviewAgentAdapter
GitHubVersionControlProvider
workflow_service
workflows router
User
ReviewAgentAdapter
GitHubVersionControlProvider
workflow_service
workflows router
User
POST /workflows {workflow_type: auto_execution, source_workflow_id}
create_workflow, context = source workflow's development-stage result
run generate_code (CodeGenerationAgent)
create_branch(repo, name, base)
commit_files(repo, branch, files, message)
run_tests (poll repo's own CI via VCS, see Risk R1)
create_pull_request(repo, branch, base, title, body)
PullRequest(id, html_url)
ai_pr_review, subject = resolve_pr_subject(pr_id) ← existing helper, unused until now
real AIAnalysisResult from a real diff
status = completed
This is the actual fix to the Review bug
resolve_pr_subject() already exists in review_adapter.py today, fully built, currently called from nowhere in the Workflow path (only the standalone PR-investigate flow uses it). Auto Execution's create_pull_request stage is the first place in the Workflow engine that ever produces a real pr:<uuid> — so wiring ai_pr_review to call resolve_pr_subject(pr_id) instead of resolve_freetext() is a one-line change that finally makes Review's own contract true. No change to Review itself, exactly as required.

05
Database changes
Table	Change	Default	Why
workflows	+ workflow_type VARCHAR(32) NOT NULL	'legacy_sdlc'	Existing rows get a distinct, frozen type — their stage set/behavior never silently changes. 'planning' is a new type used only by workflows created after this ships.
workflows	+ source_workflow_id UUID NULL REFERENCES workflows(id)	NULL	Only relational addition needed — links an Auto-Execution-from-blueprint workflow back to the Planning workflow it executes.
workflows	status value vocabulary extended	—	No column change — already a string column. + awaiting_approval/approved/rejected.
runs, agent_steps	None	—	workflow_stage is already free text; new stage ids (engineering_review, generate_code, …) need no migration. result is already JSON — new schemas (EngineeringReadinessReport etc.) fit without a new table.
Both new columns are additive with safe defaults — a single forward-only Alembic migration, no backfill logic, no data transformation.

06
API changes
Endpoint	Change
POST /workflows	+ optional workflow_type: "planning"|"auto_execution" = "planning", + optional source_workflow_id, + optional entry_point: "from_blueprint"|"from_prompt" (auto_execution only)
POST /workflows/:id/continue	Unchanged request/response — internally now type-aware
POST /workflows/:id/approve	new
POST /workflows/:id/reject	new
GET /workflows	+ optional workflow_type filter, mirrors existing status filter param exactly
GET /workflows/:id	Response += workflow_type, source_workflow_id; stages[] shape unchanged — already generic, just returns whichever list the registry produces
07
Domain model / stage interfaces
New: a StageDefinition (stage_id, label, goal, requires_prior_context) and a WORKFLOW_TYPE_STAGES: dict[str, tuple[StageDefinition, ...]] registry in workflow_service.py, replacing the single hardcoded STAGES tuple. Keys: legacy_sdlc (today's exact 4 stages, untouched), planning, auto_execution_from_blueprint, auto_execution_from_prompt.

Stage id	Agent	Status	Notes
planning	PlanningAgent	reused	Unchanged
development	DevelopmentAgent	reused	Unchanged
testing	TestPlanningAgent	reused	Unchanged
engineering_review	EngineeringReviewAgent	new	No graph tools of its own — synthesis-only over the prior 3 stages' structured JSON results (not prose), fed via the existing freetext subject path so no new Subject/resolver type is needed. Output: EngineeringReadinessReport.
generate_code	CodeGenerationAgent	new · highest risk	LLM-backed, only genuinely new AI capability in this proposal
create_branch	CreateBranchAgent	new	Deterministic — no LLM call, wraps VCS.create_branch()
commit_changes	CommitChangesAgent	new	Deterministic — wraps VCS.commit_files()
run_tests	RunTestsAgent	new · highest risk	See Risk R1 — recommend polling the repo's own CI, not an in-app sandbox
create_pull_request	CreatePullRequestAgent	new	Wraps VCS.create_pull_request()
ai_pr_review	ReviewAgentAdapter	reused, unchanged	Now correctly fed resolve_pr_subject() — see §04
create_branch/commit_files/create_pull_request are added to GitHubVersionControlProvider directly — not to the shared IVersionControlProvider interface — matching the exact precedent already set by post_pull_request_comment in this codebase (added for the same reason: LocalGitVersionControlProvider has no PR/branch-push concept, confirmed by reading app/integrations/interfaces.py, which currently only declares 4 read methods).

08
Approval flow
Planning: engineering_review completes → status=awaiting_approval → human calls /approve or /reject (new endpoints, identical request/response shape to the existing /continue). Auto-Execution "from blueprint" reads source_workflow_id's development-stage result the same way build_stage_context() already reads any prior stage's result today — no new context-passing mechanism, just a cross-workflow instead of cross-stage read.

09
Migration strategy
One Alembic migration: add both columns, backfill workflow_type='legacy_sdlc' on existing rows via the column default (no data script needed).
legacy_sdlc keeps today's exact STAGES tuple as its own registry entry — old in-flight and completed workflows are byte-for-byte unaffected.
NewWorkflowPage always creates workflow_type='planning' going forward (the default), never legacy_sdlc — that type only ever exists on rows created before this ships.
10
Risks
R1 — Test execution has no existing infrastructure
run_tests is the single largest net-new capability: no sandbox, no CI runner exists in this app today. Recommend: trigger and poll the target repo's own CI (GitHub Actions) via the VCS provider rather than building an in-app code-execution sandbox — reuses infrastructure that already exists outside this app instead of building and securing a new one.

R2 — Code generation is a real repo-write, much higher blast radius than a planning document
Recommend: generate_code/commit_changes only ever target a dedicated branch, never the default branch; treat the actual branch-push and PR-creation as an explicit, separately-confirmed action in the UI, not something that fires automatically the instant a workflow reaches that stage.

R3 — LLM-generated code correctness
Unlike Planning/Development/Testing (text artifacts a human reads before anything happens), generated code is executable. run_tests is the only safety net before a PR is opened — R1 and R3 are coupled; do not ship code-gen without real test execution.

R4 — "From prompt" entry runs the full pipeline twice as many stages as "from blueprint"
Cost/latency implication of choosing to reuse Planning+Development rather than inventing a shortcut — flagged, not blocking.

R5 (mitigated by design) — Backward compatibility
legacy_sdlc isolation (§09) means zero risk to existing workflows; this is the reason that type exists at all rather than mapping old rows onto planning.

11
Backward compatibility
Every existing workflow row, every existing endpoint call shape, and every existing frontend component keeps working unmodified. The only behavior change for pre-existing data is additive (new nullable/defaulted columns). No existing test in the current suite exercises workflow_type, so none should break.

12
Rollback strategy
Migration is purely additive — reverting the backend deploy requires no down-migration data repair. Feature can also be fully hidden without any backend rollback: removing the type selector from NewWorkflowPage (one component) makes every new workflow planning-only again, with Auto-Execution code simply unreached.

—
Implementation plan (file-by-file, by phase — for the follow-up approval, not this one)
1
Shared workflow infrastructure
app/services/workflow_service.py (StageDefinition + WORKFLOW_TYPE_STAGES, type-aware advance_workflow), app/models/workflow.py (+2 columns), new Alembic revision, app/api/v1/routers/workflows.py (type-aware, + approve/reject).
2
Planning workflow
Wire WORKFLOW_TYPE_STAGES["planning"] = planning/development/testing/engineering_review. No new agents yet — proves the engine generalization with 3 already-reused agents.
3
Engineering Review
New app/agents/engineering_review/{agent.py, schemas.py, manifest.py, prompts/engineering_review.md} — mirrors the existing testing/ folder shape exactly, minus its own graph tools (synthesis-only over prior stages' JSON).
4
Workflow approvals
/approve /reject endpoints; frontend: NewWorkflowPage type selector (Planning recommended/default), a new small approval banner on WorkflowPage for status===awaiting_approval (sibling of, not a rewrite of, ApprovalGateBanner), Dashboard split counts (group one existing listWorkflows() call client-side by type, same pattern as Run History's grouping).
5
Execution workflow (scaffold)
WORKFLOW_TYPE_STAGES["auto_execution_from_blueprint"] / ["auto_execution_from_prompt"]; source_workflow_id context-read in build_stage_context().
6
Git integration
app/integrations/github.py: + create_branch/commit_files/create_pull_request on GitHubVersionControlProvider (matches post_pull_request_comment precedent). New app/agents/code_generation/, create_branch/commit_changes as thin deterministic agents.
7
Testing execution
Per Risk R1 — CI-polling integration, not a sandbox. Biggest unknown; scope this phase on its own once reached.
8
PR Review integration
One-line change: ai_pr_review stage calls resolve_pr_subject(pr_id) instead of resolve_freetext(). review_adapter.py itself: unchanged, as required.
9
Dashboard / UI
Auto-Execution artifact cards as new named exports in the existing StageResultDetails.tsx (same file, same pattern as Planning/Development/Testing — not a new module).
10
Migration
Deploy migration, verify legacy_sdlc rows unaffected via existing Run History / Workflow pages, then flip NewWorkflowPage's default to Planning.